package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Light / Dark Theme "Natural Tones" Color Palette
val NaturalIvory = Color(0xFFFBFAF5)      // Page Background
val NaturalCharcoal = Color(0xFF1C1C17)   // Deep On-background text
val NaturalSage = Color(0xFF55624C)       // Deep Olive / Primary Sage green (Active elements)
val OnNaturalSage = Color(0xFFFFFFFF)     // White text on primary
val PaleSage = Color(0xFFE4E9D6)          // Soft sage card bg (Detected & featured highlights)
val OnPaleSage = Color(0xFF1A1C16)
val MediumSage = Color(0xFFC8D1B3)        // Midtone sage for accents/borders
val NaturalTaupe = Color(0xFFE4E2D3)      // Warm grey / Taupe for borders & inactive elements
val NaturalGrey = Color(0xFF44483D)       // Secondary text label colors
val NaturalNavBg = Color(0xFFF3F1E8)      // M3 Bottom Nav Background
val ActiveNavPill = Color(0xFFDDE5C9)     // Highlight indicator for Active Bottom Nav Bar
val RequiredExtrasBg = Color(0xFFF7F9EF)  // Soft warm tint for lists/checklists

// Darker shades for secondary usage
val DarkSage = Color(0xFF1E2F1C)
val LightIvory = Color(0xFFFFFFFC)

// Tertiary tones (Clay, Honey, Amber)
val WarmClay = Color(0xFF7D5F3D)
val WarmClayContainer = Color(0xFFF4EDE4)
